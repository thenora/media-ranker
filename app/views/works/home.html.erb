<section class="spotlight">
  <h2 class="spotlight__header">
    <span class="spotlight__header--prefix">Media Spotlight: </span><%= link_to @spotlight.title, work_path(@spotlight.id), class: "spotlight__link-to" %> by <%= @spotlight.creator %> 
  </h2>
  <p class="spotlight__description">
    <%= @spotlight.votes.count%> votes - <%= @spotlight.description%> 
  </p>  
</section>

<hr class="root__hr" />

<section class="top-ten__container">
  <section class="top-ten__list-container">
    <h2 class="top-ten__header">
      Top Movies
    </h2>
    <!-- TODO This could be a partial for each type -->
    <ul class="list-group top-ten__list">
      <% if @movies.empty? %>
          <li class="list-group-item">Oops! There are no movies. Add one?</li>
        <% else %>
          <% @movies.each do |movie| %> 
            <li class="list-group-item">
            <h4>
              <%= link_to movie.title, work_path(movie.id) %>
              <small class='top-ten__creator'>by <%= movie.creator %></small>
            </h4>
            <p><%= movie.votes.count %> votes</p>
          </li>
          <% end %>
      <% end %>
      
      </ul>

  </section>
    <section class="top-ten__list-container">
    <h2 class="top-ten__header">
      Top Books
    </h2>
    <ul class="list-group top-ten__list">
      <% if @books.empty? %>
          <li class="list-group-item">Oops! There are no books. Add one?</li>
        <% else %>
          <% @books.each do |book| %>
            <li class="list-group-item">
            <h4>
              <%= link_to book.title, work_path(book.id) %>
              <small class='top-ten__creator'>by <%= book.creator %></small>
            </h4>
            <p><%= book.votes.count %> votes</p>
          </li>
          <% end %>
      <% end %>
      
      </ul>

  </section>
  <section class="top-ten__list-container">
    <h2 class="top-ten__header">
      Top Albums
    </h2>
    <ul class="list-group top-ten__list">
      <% if @albums.empty? %>
          <li class="list-group-item">Oops! There are no albums. Add one?</li>
        <% else %>
          <% @albums.each do |album| %>
            <li class="list-group-item">
            <h4>
              <%= link_to album.title, work_path(album.id) %>
              <small class='top-ten__creator'>by <%= album.creator %></small>
            </h4>
            <p><%= album.votes.count %> votes</p>
          </li>
          <% end %>
      <% end %>
      
      </ul>

  </section>
    
</section>
