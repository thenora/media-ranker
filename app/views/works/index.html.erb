<h2>List of Works</h2>
  <!-- # TODO Add partials -->
  <section class="media-table">
    <h4>Albums</h4>
    <table class="table">
      <thead>
        <tr>
          <th width=80>Votes</th>
          <th>Title</th>
          <th>Created By</th>
          <th width=100>Published</th>
          <th width=100>Upvote</th>
        </tr>
      </thead>
      <tbody>
        <% @albums.each do |album| %>
          <tr>
            <td> 1 <!-- TODO add votes --> </td>
            <td><%= link_to album.title, work_path(album.id) %></td>
            <td><%= album.creator %></td>
            <td><%= album.publication_year%></td>
            <td> Upvote <!-- TODO add votes <a class="btn btn-primary" rel="nofollow" data-method="post" href="/works/699/upvote"> --> </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </section>
  <section class="media-table">
    <h4>Books</h4>
    <table class="table">
      <thead>
        <tr>
          <th width=80>Votes</th>
          <th>Title</th>
          <th>Created By</th>
          <th width=100>Published</th>
          <th width=100>Upvote</th>
        </tr>
      </thead>
      <tbody>
          <% @books.each do |book| %>
          <tr>
            <td> 1 <!-- TODO add votes --> </td>
            <td><%= link_to book.title, work_path(book.id) %></td>
            <td><%= book.creator %></td>
            <td><%= book.publication_year%></td>
            <td> Upvote <!-- TODO add votes <a class="btn btn-primary" rel="nofollow" data-method="post" href="/works/699/upvote"> --> </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </section>
  <section class="media-table">
    <h4>Movies</h4>
    <table class="table">
      <thead>
        <tr>
          <th width=80>Votes</th>
          <th>Title</th>
          <th>Created By</th>
          <th width=100>Published</th>
          <th width=100>Upvote</th>
        </tr>
      </thead>
      <tbody>
        <% @movies.each do |movie| %>
          <tr>
            <td> 1 <!-- TODO add votes --> </td>
            <td><%= link_to movie.title, work_path(movie.id) %></td>
            <td><%= movie.creator %></td>
            <td><%= movie.publication_year%></td>
            <td> Upvote <!-- TODO add votes <a class="btn btn-primary" rel="nofollow" data-method="post" href="/works/699/upvote"> --> </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </section>

<%= link_to "View top media", root_path, class: "btn btn-secondary" %> 
<%= link_to "Add a new work", new_work_path, class: "btn btn-primary" %>